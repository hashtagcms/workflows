<?php

namespace {{ namespace }};

use HashtagCms\Workflows\Contracts\WorkflowHandlerInterface;
use HashtagCms\Workflows\Engine\WorkflowContext;
use HashtagCms\Workflows\Engine\WorkflowResponse;

/**
 * {{ class }}
 *
 * Register this handler so the engine can resolve it. In a service provider
 * (e.g. app/Providers/AppServiceProvider@boot):
 *
 *     \HashtagCms\Workflows\Facades\Workflows::register(
 *         '{{ alias }}',
 *         \{{ namespace }}\{{ class }}::class
 *     );
 *
 * Then execute it via the API or facade with the alias "{{ alias }}".
 */
class {{ class }} implements WorkflowHandlerInterface
{
    public function handle(WorkflowContext $context): WorkflowResponse
    {
        // Read inputs from the client payload:
        // $value = $context->get('key', 'default');

        return WorkflowResponse::make()
            ->setSuccess(true, 'Workflow executed.')
            ->toast('Replace this with your business logic.', 'success');
    }
}
